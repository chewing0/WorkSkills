# 过期删除策略

## 过期时间

## 过期删除策略

定时删除：在设置key的过期时间时，同时创建一个定时事件，当时间到达时，由事件处理器自动执行key的删除操作。
惰性删除：不主动删除，每次访问key时，检查key是否过期，如果过期则删除key。
定期删除：每个一段时间随机从数据库中取出一定数量的key进行检查，并删除其中的过期key。
Redis通常使用惰性删除+定期删除的策略。定时删除的时间间隔？和抽查的数量？


# 内存淘汰策略

运行的最大内存在Redis的config文件中可以进行配置。只有Redis的运行内存达到了我们设置的最大运行内存，才会触发内存淘汰策略。64位系统通常maxmemory默认设置为0，表示没有内存大小限制；32位系统通常默认为3G，因为32位机器支持的最大内存为4GB。

## 内存淘汰的策略

